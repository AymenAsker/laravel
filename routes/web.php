<?php

use App\Http\Controllers\Customer\SocialLoginController;
use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| Here is where you can register web routes for your application. These
| routes are loaded by the RouteServiceProvider within a group which
| contains the "web" middleware group. Now create something great!
|
*/

//Route::get('/', function () {
//    return view('welcome');
//});
//Route::get('auth/{provider}/redirect',[])->name('auth.socialite.redirect');
//Route::get('auth/{provider}/callback',[])->name('auth.socialite.callback');
//Route::controller(SocialLoginController::class)->group(function () {
//    Route::get('auth/{provider}/redirect', 'redirect');
//    Route::get('auth/{provider}/callback', 'callback');
//});
